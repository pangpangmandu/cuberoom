/* eslint-disable import/prefer-default-export */

/* eslint-disable no-unused-vars */
const hallUpperImages = {
  x: 800,
  y: 600,
};

/**
 * 이미지 인터렉션 디버깅용
 */
const nearImages = {
  x: 16 * 53 + 1,
  y: 16 * 53 + 10,
};

const startPoint = {
  x: 16 * 19 + 12,
  y: 16 * (68 - 9) - 8,
};

export const playerStartPos = startPoint;
